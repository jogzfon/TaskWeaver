name: sql_management_system
enabled: true
required: false
description: >-
  Pull data from a SQL database. 
  This plugin takes user requests when obtaining data from database is explicitly mentioned.
  Otherwise, confirm with the user if they want to pull data from this database.
  The data from this database can only be used for sql management system.
examples: |-
  df, description = SQLManagementSystem("pull data from time_series table")

parameters:
  - name: query
    type: str
    required: true
    description: >-
      This is the query in natural language that the user wants to get data from database.
      If any specific column or value is mentioned, make sure to include them in the query, 
      exactly in the right format or form.

returns:
  - name: df
    type: pandas.DataFrame
    description: This is the dataframe containing the data from the database.
  - name: description
    type: str
    description: This is a string describing the data pulled from the database and the SQL query used.

configurations:
  api_type: gemini
  api_key: AIzaSyA0fCGtlAyxF_s_dBObsnL70xocI3GJlTE
  api_version: gemini-pro
  sqlite_db_path: sqlite:///../../../../sample_data/enrollment_system.db