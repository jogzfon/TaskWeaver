name: student_management
enabled: true
required: false
description: >-
  Pull data from a SQL database. 
  This plugin takes user requests when obtaining data from database is explicitly mentioned.
  Otherwise, confirm with the user if they want to pull data from this database.
  The data from this database can only be used for sql management system.
examples: |-
  result = student_management("Get all data")

parameters:
  - name: query
    type: str
    required: true
    description: >-
      This is the query in natural language that the user wants to get data from database.
      If any specific column or value except for passwords is mentioned, make sure to include them in the query, 
      exactly in the right format or form.

returns:
  - name: result
    type: str
    description: This is a string result after the data pulled from the database and the SQL query used.

configurations:
  api_type: gemini
  api_key: AIzaSyA0fCGtlAyxF_s_dBObsnL70xocI3GJlTE
  api_version: gemini-pro
  sqlite_db_path: sqlite:///../../../../sample_data/student_enrollment.db